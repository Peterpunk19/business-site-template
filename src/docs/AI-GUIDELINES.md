This project is a reusable business website template.

Rules:

- Never hardcode business information.
- Business data must come from /config.
- Components must be reusable.
- TypeScript strict mode.
- Mobile-first.
- Accessibility is required.
- SEO metadata must come from business configuration.
- Features must be isolated by domain.
- Validate server inputs with Zod.
